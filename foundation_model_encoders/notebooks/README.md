# Legacy notebooks

The notebooks in `legacy/` are retained for provenance and interactive inspection. They may contain assumptions from the original exploratory workflow and are not the primary execution interface.

Use the path-configurable Python scripts and `launcher.py` for reproducible runs. Generated manifests and embeddings can still be loaded into these notebooks for exploratory visualization after adapting notebook-local cells as needed.
